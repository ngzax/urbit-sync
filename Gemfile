source 'https://rubygems.org'

# We're using the bleeding edge, latest version, because the latest gem
# (shown below) is outdated. Should probably switch to a specific gem version
# once that is fixed.
# gem 'filewatcher', git: 'https://github.com/thomasfl/filewatcher'

# gem 'filewatcher'
gem 'filewatcher', "2.1.0"
